# 本地开发说明

## 🛠️ 本地环境设置

### 1. 克隆项目

```bash
git clone https://github.com/your-username/your-repo.git
cd your-repo
```

### 2. 确保 Python 环境

需要 Python 3.7+：

```bash
python --version
# 或
python3 --version
```

### 3. 测试脚本

```bash
# Windows
运行更新README.bat

# Linux/Mac
python 更新README资料.py
```

## 🔧 脚本配置

### 修改路径

如果项目不在默认路径，可以修改 `更新README资料.py` 中的路径：

```python
# 默认路径（本地开发时）
BASE_PATH = Path(r"d:\学习\中山大学智能工程学院本科生课程作业")

# 或者使用相对路径（推荐）
BASE_PATH = Path(__file__).parent
```

脚本会自动检测：
1. 如果在 Git 仓库中，使用项目根目录
2. 如果在 GitHub Actions 中，使用工作区路径
3. 否则使用默认本地路径

### 自定义分类

可以在脚本的 `FILE_CATEGORIES` 字典中添加更多关键词：

```python
FILE_CATEGORIES = {
    '作业': ['作业', 'homework', 'hw', 'assignment'],
    '实验': ['实验', 'exp', 'lab', '实验课'],
    # 添加你的自定义分类
    '笔记': ['笔记', 'note', 'notes'],
    # ...
}
```

## 🧪 测试

### 测试单个课程

修改 `main()` 函数：

```python
def main():
    # 只测试特定课程
    course_dir = BASE_PATH / "图像处理"
    update_readme(course_dir)
```

### 测试文件分类

可以创建一个测试目录，放入不同类型的文件，然后运行脚本查看分类结果。

## 📝 开发流程

1. **本地修改**：修改脚本或添加文件
2. **测试**：运行脚本测试效果
3. **提交**：提交更改
4. **推送**：推送到 GitHub，触发 Actions

## 🐛 调试

### 查看详细输出

脚本会输出处理过程：

```
开始更新课程README文件...

找到 28 个课程目录

✅ 图像处理: README已更新
✅ 分布式计算: README已更新
...
```

### 常见问题

1. **路径错误**：检查 `BASE_PATH` 是否正确
2. **编码问题**：确保文件使用 UTF-8 编码
3. **权限问题**：确保有读写权限

## 🔄 Git Hooks（可选）

如果你想在每次提交前自动运行脚本，可以设置 pre-commit hook：

```bash
# 创建 hook 文件
cat > .git/hooks/pre-commit << 'EOF'
#!/bin/bash
python 更新README资料.py
git add */README.md
EOF

# 添加执行权限
chmod +x .git/hooks/pre-commit
```

**注意**：这会在每次提交前运行，可能会增加提交时间。

## 📦 依赖

脚本只使用 Python 标准库，无需安装额外依赖：

- `os`
- `re`
- `pathlib`
- `collections`
- `typing`

## 🚀 部署到 GitHub

1. 确保 `.github/workflows/update-readme.yml` 存在
2. 确保脚本路径正确
3. 推送代码到 GitHub
4. 检查 Actions 是否正常运行

## 💡 提示

- 在本地测试后再推送到 GitHub
- 查看 GitHub Actions 日志了解运行情况
- 如果 Actions 失败，检查错误信息并修复
